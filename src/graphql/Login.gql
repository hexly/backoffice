mutation auth($creds: Credentials!) {
  login(input: $creds) {
    token
    success
    reason
    principal {
      username
      identityId
      memberId
      member {
        displayName
        contacts {
          id
        }
      }
    }
  }
}
